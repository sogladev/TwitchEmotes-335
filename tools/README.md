## Quick Python setup for tools

1. Install [uv](https://github.com/astral-sh/uv) if you don’t have it.
2. Create and sync your virtual environment:
   ```zsh
   uv venv .venv
   uv pip install -r requirements.txt
   ```